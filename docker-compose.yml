services:
  searxng:
    container_name: searxng
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - 'SEARXNG_BASE_URL=https://search-lan.0mod.com/'
      - GRANIAN_WORKERS=2
